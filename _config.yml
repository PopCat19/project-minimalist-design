# _config.yml
#
# Purpose: Jekyll configuration for GitHub Pages, excludes conventions from processing
#
# This module:
# - Excludes convention files to avoid Liquid syntax errors

exclude:
  - conventions/
  - changelog_archive/
  - "CHANGELOG-*.md"
